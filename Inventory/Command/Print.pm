package Inventory::Command::Print;

use strict;
use warnings;

use Inventory;

class Inventory::Command::Print {
    is => 'Inventory::Command',
    #has => [
    #    printer => { is => 'String', doc => 'Printer name' },
    #],
};

1;


